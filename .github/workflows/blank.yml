            - name: nod AI Compliance Gatekeeper
  # You may pin to the exact commit or the version.
  # uses: mraml/nod@dbeb3c1fc47043afc18b5ecaac1dc6b8dc8f0883
  uses: mraml/nod@v2.2.0
  with:
    # Path to the spec file to audit (e.g., ai-spec.md)
    target: 
    # Path to rules file or directory. Defaults to "defaults/" or "rules.yaml".
    rules: # optional, default is 
    # Enable strict mode (checks content length)
    strict: # optional, default is true
    # Minimum severity to fail the build (MEDIUM, HIGH, CRITICAL)
    min_severity: # optional, default is HIGH
    # Output format (text, json, sarif, compliance)
    output_format: # optional, default is text
    # File path to save the output (e.g. nod-results.sarif)
    output_file: # optional, default is 
          
